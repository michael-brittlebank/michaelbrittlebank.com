language: node_js

node_js:
  - "9.11.1"

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.6.0
  - export PATH="$HOME/.yarn/bin:$PATH"

before_script:
  - yarn
  - yarn run build

script:
  - yarn run test