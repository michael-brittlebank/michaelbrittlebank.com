<header>
    <div class="grid-container row">
        <div class="col-sm-12 show-for-small text-right position-relative">
            <a id="header-mobile-menu" href="#modal-mobile-menu">
                <i class="fa fa-bars" aria-hidden="true"></i>
            </a>
        </div>
        <nav class="col-sm-12 text-right hide-for-small">
            <ul class="standard-menu">
                {{#each menuItems}}
                    <li class="{{class}}">
                        <a href="{{url}}" class="{{#if isActive}}active{{/if}}">{{title}}</a>
                        {{#if children}}
                            <ul class="sub-menu">
                                {{#each children}}
                                    <li class="{{class}}">
                                        <a href="{{url}}">{{title}}</a>
                                    </li>
                                {{/each}}
                            </ul>
                        {{/if}}
                    </li>
                {{/each}}
            </ul>
        </nav>
    </div>
</header>