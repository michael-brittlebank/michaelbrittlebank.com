(function(){

    var that = app.views.portfolio.page,
        $ = jQuery;

    this.init = function(){
        if($('#page-portfolio').length > 0){

        }
    };

}).apply(app.views.portfolio.page);